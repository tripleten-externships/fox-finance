FIREBASE_PRIVATE_KEY='private_key_value'
FIREBASE_CLIENT_EMAIL='client@email.com'
FIREBASE_PROJECT_ID='project-id'
FIREBASE_API_KEY='your-api-key-here'

# Upload Token Authentication
# Secret key for signing upload authentication JWTs
# IMPORTANT: Generate a strong random secret for production (e.g., using `openssl rand -base64 32`)
# This secret is used to sign both auth tokens (in upload links) and bearer tokens (for upload requests)
UPLOAD_TOKEN_SECRET='your-secret-key-here-change-in-production'

# Database Configuration
# Copy this file to .env and update the values as needed


# For production, use a secure password and appropriate host
# DATABASE_URL="postgresql://username:secure_password@production_host:5432/fox-finance?schema=public&connection_limit=20&pool_timeout=10"