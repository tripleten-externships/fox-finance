FIREBASE_PRIVATE_KEY='private_key_value'
FIREBASE_CLIENT_EMAIL='client@email.com'
FIREBASE_PROJECT_ID='project-id'

# Database Configuration
# Copy this file to .env and update the values as needed

# PostgreSQL connection string for Prisma with connection pooling
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=SCHEMA&connection_limit=20&pool_timeout=10
# connection_limit: Maximum number of connections in the pool (default: 20)
# pool_timeout: Seconds to wait for an available connection (default: 10)
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/fox-finance?schema=public&connection_limit=20&pool_timeout=10"

# For production, use a secure password and appropriate host
# DATABASE_URL="postgresql://username:secure_password@production_host:5432/fox-finance?schema=public&connection_limit=20&pool_timeout=10"